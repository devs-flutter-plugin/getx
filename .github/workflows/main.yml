name: CI

on:
  push:
    branches: [master]

jobs:
  test:
    runs-on:
      - self-hosted
      - devs-flutter-plugin-ci
    defaults:
      run:
        working-directory: ./

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.2'
          channel: stable
          cache: true

      - name: Toolchain
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Test
        run: flutter test --coverage

      - name: Upload coverage
        uses: codecov/codecov-action@v7

# Public repositories are explicitly enabled for the organization self-hosted runner group.
